@using Microsoft.AspNetCore.Http;
@inject IHttpContextAccessor HttpContextAccessor

<ul class="navbar-nav">
    @{
        ViewBag.Data = @HttpContextAccessor.HttpContext.Session.GetString("Name");
    }
    @if(ViewBag.Data != null)
    {
        @* <li class="nav-item">
            <a id="manage" class="nav-link text-dark" asp-controller="Home" asp-action="Index" title="Manage">@ViewBag.Data </a>
        </li>
        <li class="nav-item">
            <form>
                <button id="logout" type="submit" class="nav-link btn btn-link text-dark" asp-controller="Login" asp-action="logout">Logout</button>
            </form>
        </li> *@
        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle text-dark" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                @ViewBag.Data
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                <a id="manage" class="nav-link text-dark" asp-controller="Login" asp-action="details" title="Manage">DashBoard </a>
                <form>
                    <button id="logout" type="submit" class="nav-link btn btn-link text-dark" asp-controller="Login" asp-action="logout">Logout</button>
                </form>
            </div>
            
        </li>
    }
    else
    {
        <li class="nav-item">
            <a id="login" class="nav-link text-dark" asp-controller="Login" asp-action="login">Login </a>
        </li>
    }
</ul>